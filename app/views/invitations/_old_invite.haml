#invitation
  .container
    %h1
      = "Hi there, #{@invitation.invitee.short_name}!"

    %p
      = @invitation.inviter.short_name
      would like to invite you to find out more about Panga.

    %p
      %em
        (Hey Teq, we need to write this page. Lets do the content first and then I can make it pretty.)

    .upcoming-workshops
      %h2 Upcoming Workshops
      %ul
        - @sessions.each do |session|
          %li.workshop
            .date= session.start_on.to_formatted_s(:with_weekday)
            .time
              = session.start_at.to_formatted_s(:time_only)
              = "-"
              = session.end_at.to_formatted_s(:time_only)

      .actions
        = link_to "Submit my availability", '#', class: 'btn btn-primary'
