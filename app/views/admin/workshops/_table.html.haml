%table.table
  %thead
    %tr
      %th{scope: "col"} Name
      %th{scope: "col"} Actions
  %tbody
    - @records.each do |record|
      %tr
        %td= record.name
        %td
          = edit_link(record)
