%table.table
  %thead
    %tr
      %th{scope: "col"} Person
      %th{scope: "col"} Email
      %th{scope: "col"} Roles
      %th{scope: "col"} Actions
  %tbody
    - @records.each do |record|
      %tr
        %td= record.person
        %td= record.email
        %td= record.roles.map(&:name).join(", ")
        %td
          = edit_link(record)
